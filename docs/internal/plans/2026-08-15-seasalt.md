# seasalt Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** bash に fish 風のインライン自動補完とフォルダ別履歴管理を提供する Rust 製プラグイン seasalt を実装する。

**Architecture:** Rust のシングルバイナリ `seasalt`(サブコマンド方式)と ble.sh 統合 bash スニペットから構成。ストレージは SQLite(WAL、デーモンなしの直接アクセス)。ble.sh の `ble/complete/auto-complete/source:*` 拡張ポイントに自作ソースを挿入し、`blehook PREEXEC/PRECMD` で履歴記録を行う。atuin とは独立ストアで共存。

**Tech Stack:** Rust (edition 2021)、`rusqlite`(bundled)、`clap`(derive)、`dirs`、`anyhow`、bash + ble.sh (0.4.0-devel3)

**Spec:** [docs/internal/specs/2026-08-15-seasalt-design.md](../specs/2026-08-15-seasalt-design.md)

## Global Constraints

- デーモンは作らない。SQLite 直接アクセス方式。
- DB パス: `$SEASALT_DATA_DIR`(未設定時 `$XDG_DATA_HOME/seasalt/`、未設定時 `~/.local/share/seasalt/`)の `history.sqlite3`
- 検証コマンド(各タスクのコミット前に実行): `cargo fmt && cargo check && cargo clippy && cargo test`
- コミットは毎回ユーザーの承認を得てから行う。コミットメッセージは英語・簡潔に。
- ファイル削除には `rm` を使わず `trash put` を使う。
- スコープ仕様(変更不可): suggest は cwd 完全一致 → 親ディレクトリ(近い順)→ グローバルの順で prefix 一致(大文字小文字無視)の最新を返す。各スコープ内は `started_at DESC, id DESC` で最新優先。
- エラー時は静かに失敗する: suggest/record/exit は失敗しても stderr に出力せず、非 0 で終了する。

---

### Task 1: SQLite ストレージ層

**Files:**
- Create: `Cargo.toml`
- Create: `.gitignore`
- Create: `src/lib.rs`
- Create: `src/db.rs`
- Create: `src/main.rs`(最小の雛形)
- Test: `tests/db_test.rs`

**Interfaces:**
- Produces (later tasks depend on these exact signatures):
  - `db::open(path: &Path) -> anyhow::Result<Connection>` — DB を開き(存在しなければ作成)、スキーマを初期化。ファイル DB では WAL を有効化(in-memory ではスキップ)。
  - `db::init(conn: &Connection) -> anyhow::Result<()>` — `CREATE TABLE IF NOT EXISTS` + インデックス作成(冪等)
  - `db::insert_history(conn: &Connection, cwd: &str, cmd: &str, started_at: i64, session: &str) -> anyhow::Result<i64>` — 行を挿入し rowid を返す
  - `db::update_exit_code(conn: &Connection, session: &str, id: i64, code: i64) -> anyhow::Result<()>`
  - `db::default_db_path() -> anyhow::Result<PathBuf>` — 上記 Global Constraints の優先順位で解決
  - `pub struct HistoryEntry { pub id: i64, pub cwd: String, pub cmd: String, pub exit_code: Option<i64>, pub started_at: i64 }`

- [ ] **Step 1: プロジェクト雛形を作成**

```toml
# Cargo.toml
[package]
name = "seasalt"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
dirs = "6"
rusqlite = { version = "0.37", features = ["bundled"] }
```

```gitignore
# .gitignore
/target
/result
.direnv
```

```rust
// src/lib.rs
pub mod db;

// src/main.rs (最小雛形)
fn main() {}
```

- [ ] **Step 2: 失敗するテストを書く**

```rust
// tests/db_test.rs
use rusqlite::Connection;
use seasalt::db;

#[test]
fn insert_and_update_exit_code_roundtrip() {
    let conn = Connection::open_in_memory().unwrap();
    db::init(&conn).unwrap();

    let id = db::insert_history(&conn, "/tmp/a", "echo hello", 1000, "s1").unwrap();
    db::update_exit_code(&conn, "s1", id, 42).unwrap();

    let mut stmt = conn
        .prepare("SELECT cwd, cmd, exit_code, started_at, session FROM history WHERE id = ?1")
        .unwrap();
    let row = stmt
        .query_row([id], |r| {
            Ok((
                r.get::<_, String>(0)?,
                r.get::<_, String>(1)?,
                r.get::<_, Option<i64>>(2)?,
                r.get::<_, i64>(3)?,
                r.get::<_, String>(4)?,
            ))
        })
        .unwrap();
    assert_eq!(row, ("/tmp/a".into(), "echo hello".into(), Some(42), 1000, "s1".into()));
}

#[test]
fn init_is_idempotent() {
    let conn = Connection::open_in_memory().unwrap();
    db::init(&conn).unwrap();
    db::init(&conn).unwrap(); // 2回目もエラーにならない
}

#[test]
fn insert_returns_increasing_ids() {
    let conn = Connection::open_in_memory().unwrap();
    db::init(&conn).unwrap();
    let a = db::insert_history(&conn, "/x", "a", 1, "s").unwrap();
    let b = db::insert_history(&conn, "/x", "b", 2, "s").unwrap();
    assert!(b > a);
}

#[test]
fn default_db_path_respects_env_override() {
    let dir = std::env::temp_dir().join(format!("seasalt-test-{}", std::process::id()));
    std::env::set_var("SEASALT_DATA_DIR", &dir);
    let path = db::default_db_path().unwrap();
    assert_eq!(path, dir.join("history.sqlite3"));
    std::env::remove_var("SEASALT_DATA_DIR");
}

#[test]
fn update_exit_code_on_missing_row_is_ok() {
    let conn = Connection::open_in_memory().unwrap();
    db::init(&conn).unwrap();
    // 存在しない (session, id) への update はエラーにしない
    db::update_exit_code(&conn, "nope", 999, 0).unwrap();
}
```

- [ ] **Step 3: テストが失敗することを確認**

Run: `cargo test`
Expected: FAIL(コンパイルエラー / `db` モジュール未定義)

- [ ] **Step 4: 最小実装**

```rust
// src/db.rs
use std::path::{Path, PathBuf};

use anyhow::{Context, Result};
use rusqlite::Connection;

pub const SCHEMA: &str = "
CREATE TABLE IF NOT EXISTS history (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  cwd TEXT NOT NULL,
  cmd TEXT NOT NULL,
  exit_code INTEGER,
  started_at INTEGER NOT NULL,
  session TEXT
);
CREATE INDEX IF NOT EXISTS idx_history_cwd ON history(cwd);
CREATE INDEX IF NOT EXISTS idx_history_cmd ON history(cmd);
";

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct HistoryEntry {
    pub id: i64,
    pub cwd: String,
    pub cmd: String,
    pub exit_code: Option<i64>,
    pub started_at: i64,
}

pub fn default_db_path() -> Result<PathBuf> {
    let base = if let Ok(dir) = std::env::var("SEASALT_DATA_DIR") {
        PathBuf::from(dir)
    } else if let Some(data) = dirs::data_dir() {
        data.join("seasalt")
    } else {
        anyhow::bail!("cannot determine data directory; set SEASALT_DATA_DIR");
    };
    std::fs::create_dir_all(&base)
        .with_context(|| format!("cannot create data directory {}", base.display()))?;
    Ok(base.join("history.sqlite3"))
}

pub fn open(path: &Path) -> Result<Connection> {
    let conn = Connection::open(path).with_context(|| format!("cannot open {}", path.display()))?;
    if path != Path::new(":memory:") {
        conn.pragma_update(None, "journal_mode", "WAL")?;
    }
    init(&conn)?;
    Ok(conn)
}

pub fn init(conn: &Connection) -> Result<()> {
    conn.execute_batch(SCHEMA)?;
    Ok(())
}

pub fn insert_history(
    conn: &Connection,
    cwd: &str,
    cmd: &str,
    started_at: i64,
    session: &str,
) -> Result<i64> {
    conn.execute(
        "INSERT INTO history (cwd, cmd, started_at, session) VALUES (?1, ?2, ?3, ?4)",
        rusqlite::params![cwd, cmd, started_at, session],
    )?;
    Ok(conn.last_insert_rowid())
}

pub fn update_exit_code(conn: &Connection, session: &str, id: i64, code: i64) -> Result<()> {
    conn.execute(
        "UPDATE history SET exit_code = ?1 WHERE session = ?2 AND id = ?3",
        rusqlite::params![code, session, id],
    )?;
    Ok(())
}
```

- [ ] **Step 5: テストが通ることを確認**

Run: `cargo test`
Expected: PASS(5 テスト)

- [ ] **Step 6: コミット(ユーザー承認を得てから)**

```bash
git add Cargo.toml Cargo.lock .gitignore src/lib.rs src/db.rs src/main.rs tests/db_test.rs
git commit -m "feat: add sqlite history store"
```

---

### Task 2: suggest エンジン(スコープ優先順位)

**Files:**
- Create: `src/suggest.rs`
- Modify: `src/lib.rs`(`pub mod suggest;` を追加)
- Test: `tests/suggest_test.rs`

**Interfaces:**
- Consumes: `db::init`, `db::insert_history`(Task 1)
- Produces:
  - `suggest::suggest(conn: &Connection, cwd: &str, line: &str) -> anyhow::Result<Option<String>>` — スコープ順に prefix 一致の最新コマンド全文を返す。候補がなければ `None`。`line` と同一のコマンドは候補にしない。
  - `db::suggest_in_dir(conn: &Connection, cwd: &str, needle: &str) -> anyhow::Result<Option<String>>` — 特定ディレクトリ内で prefix 一致の最新 1 件
  - `db::suggest_global(conn: &Connection, needle: &str) -> anyhow::Result<Option<String>>`
  - ヘルパ: `suggest::ancestors(cwd: &str) -> Vec<String>` — 親ディレクトリを近い順に列挙(ルート `/` は含まない)。例: `/a/b/c` → `["/a/b", "/a"]`

- [ ] **Step 1: 失敗するテストを書く**

```rust
// tests/suggest_test.rs
use rusqlite::Connection;
use seasalt::db;
use seasalt::suggest;

fn seed(conn: &Connection) {
    // started_at を明示制御して「最新」の判定を確定させる
    db::insert_history(conn, "/proj/sub", "cargo build", 5000, "s").unwrap();
    db::insert_history(conn, "/proj/sub", "cargo test", 4000, "s").unwrap();
    db::insert_history(conn, "/proj", "cargo check", 3000, "s").unwrap();
    db::insert_history(conn, "/other", "cargo doc", 2000, "s").unwrap();
    db::insert_history(conn, "/home", "ls -la", 6000, "s").unwrap();
}

#[test]
fn exact_cwd_is_prioritized_over_parent() {
    let conn = Connection::open_in_memory().unwrap();
    db::init(&conn).unwrap();
    seed(&conn);

    // /proj/sub では /proj の cargo check ではなく /proj/sub の最新 (cargo build) を返す
    let got = suggest::suggest(&conn, "/proj/sub", "cargo").unwrap().unwrap();
    assert_eq!(got, "cargo build");
}

#[test]
fn parent_is_prioritized_over_global() {
    let conn = Connection::open_in_memory().unwrap();
    db::init(&conn).unwrap();
    seed(&conn);

    // /proj/deep/deeper は cwd 一致なし → 親 /proj/deep, /proj を順に探索 → /proj の cargo check
    let got = suggest::suggest(&conn, "/proj/deep/deeper", "cargo").unwrap().unwrap();
    assert_eq!(got, "cargo check");
}

#[test]
fn global_fallback_works() {
    let conn = Connection::open_in_memory().unwrap();
    db::init(&conn).unwrap();
    seed(&conn);

    // /nowhere は cwd・親に一致なし → グローバルで最新の ls -la
    let got = suggest::suggest(&conn, "/nowhere", "ls").unwrap().unwrap();
    assert_eq!(got, "ls -la");
}

#[test]
fn latest_within_scope_wins() {
    let conn = Connection::open_in_memory().unwrap();
    db::init(&conn).unwrap();
    seed(&conn);

    // 同一ディレクトリ /proj/sub 内で cargo で始まる最新 = cargo build (5000 > 4000)
    let got = suggest::suggest(&conn, "/proj/sub", "cargo").unwrap().unwrap();
    assert_eq!(got, "cargo build");
}

#[test]
fn no_match_returns_none() {
    let conn = Connection::open_in_memory().unwrap();
    db::init(&conn).unwrap();
    seed(&conn);

    assert!(suggest::suggest(&conn, "/proj/sub", "zzz_not_here").unwrap().is_none());
}

#[test]
fn suggestion_never_equals_current_line() {
    let conn = Connection::open_in_memory().unwrap();
    db::init(&conn).unwrap();
    db::insert_history(&conn, "/proj/sub", "cargo build", 5000, "s").unwrap();

    // 直前に実行して記録された同じコマンドは候補にしない
    assert!(suggest::suggest(&conn, "/proj/sub", "cargo build").unwrap().is_none());
}

#[test]
fn case_insensitive_match() {
    let conn = Connection::open_in_memory().unwrap();
    db::init(&conn).unwrap();
    db::insert_history(&conn, "/proj/sub", "CARGO BUILD", 5000, "s").unwrap();

    let got = suggest::suggest(&conn, "/proj/sub", "cargo").unwrap().unwrap();
    assert_eq!(got, "CARGO BUILD");
}

#[test]
fn ancestors_does_not_include_root() {
    assert_eq!(suggest::ancestors("/a/b/c"), vec!["/a/b".to_string(), "/a".to_string()]);
    assert!(suggest::ancestors("/").is_empty());
}

#[test]
fn empty_line_returns_none() {
    let conn = Connection::open_in_memory().unwrap();
    db::init(&conn).unwrap();
    seed(&conn);

    assert!(suggest::suggest(&conn, "/proj/sub", "").unwrap().is_none());
}
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cargo test`
Expected: FAIL(`suggest` モジュール未定義)

- [ ] **Step 3: 最小実装**

```rust
// src/db.rs に追加
pub fn suggest_in_dir(conn: &Connection, cwd: &str, needle: &str) -> Result<Option<String>> {
    if needle.is_empty() {
        return Ok(None);
    }
    let pattern = format!("{}%", escape_like(needle));
    let mut stmt = conn.prepare(
        "SELECT cmd FROM history WHERE cwd = ?1 AND cmd LIKE ?2 ESCAPE '\\'
         ORDER BY started_at DESC, id DESC LIMIT 1",
    )?;
    let mut rows = stmt.query(rusqlite::params![cwd, pattern])?;
    if let Some(row) = rows.next()? {
        return Ok(Some(row.get(0)?));
    }
    Ok(None)
}

pub fn suggest_global(conn: &Connection, needle: &str) -> Result<Option<String>> {
    if needle.is_empty() {
        return Ok(None);
    }
    let pattern = format!("{}%", escape_like(needle));
    let mut stmt = conn.prepare(
        "SELECT cmd FROM history WHERE cmd LIKE ?1 ESCAPE '\\'
         ORDER BY started_at DESC, id DESC LIMIT 1",
    )?;
    let mut rows = stmt.query(rusqlite::params![pattern])?;
    if let Some(row) = rows.next()? {
        return Ok(Some(row.get(0)?));
    }
    Ok(None)
}

fn escape_like(s: &str) -> String {
    s.chars().flat_map(|c| match c {
        '%' | '_' | '\\' => vec!['\\', c],
        other => vec![other],
    }).collect()
}
```

```rust
// src/suggest.rs
use anyhow::Result;
use rusqlite::Connection;

use crate::db;

/// 親ディレクトリを近い順に列挙(ルート自体は含まない)
pub fn ancestors(cwd: &str) -> Vec<String> {
    let mut out = Vec::new();
    let mut cur = std::path::Path::new(cwd);
    while let Some(parent) = cur.parent() {
        let s = parent.to_string_lossy();
        if s.is_empty() || s == "/" {
            break;
        }
        out.push(s.into_owned());
        cur = parent;
    }
    out
}

pub fn suggest(conn: &Connection, cwd: &str, line: &str) -> Result<Option<String>> {
    if line.trim().is_empty() {
        return Ok(None);
    }
    // スコープ 1: cwd 完全一致
    if let Some(cmd) = db::suggest_in_dir(conn, cwd, line)? {
        if cmd != line {
            return Ok(Some(cmd));
        }
        return Ok(None);
    }
    // スコープ 2: 親ディレクトリ(近い順)
    for anc in ancestors(cwd) {
        if let Some(cmd) = db::suggest_in_dir(conn, &anc, line)? {
            if cmd != line {
                return Ok(Some(cmd));
            }
            return Ok(None);
        }
    }
    // スコープ 3: グローバル
    if let Some(cmd) = db::suggest_global(conn, line)? {
        if cmd != line {
            return Ok(Some(cmd));
        }
    }
    Ok(None)
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cargo test`
Expected: PASS(9 テスト)

- [ ] **Step 5: コミット(ユーザー承認を得てから)**

```bash
git add src/lib.rs src/db.rs src/suggest.rs tests/suggest_test.rs
git commit -m "feat: add scope-prioritized suggestion engine"
```

---

### Task 3: CLI 統合(record / exit / suggest / search)

**Files:**
- Create: `src/search.rs`
- Modify: `src/lib.rs`(`pub mod search;` を追加)
- Rewrite: `src/main.rs`(clap による完全な CLI)
- Test: `tests/cli_test.rs`(ビルド済みバイナリを `env!("CARGO_BIN_EXE_seasalt")` で呼ぶ統合テスト)

**Interfaces:**
- Consumes: `db::{open, default_db_path, insert_history, update_exit_code, HistoryEntry}`, `suggest::suggest`
- Produces:
  - `search::search(conn: &Connection, cwd: Option<&str>, pattern: &str, limit: usize) -> anyhow::Result<Vec<HistoryEntry>>` — `--cwd` 指定時は cwd 完全一致のみ、未指定は全履歴から。`cmd LIKE %pattern%`(大文字小文字無視)で `started_at DESC, id DESC` 順、limit 件
  - サブコマンド仕様(実行例):
    - `seasalt record --cwd DIR --session SESS -- CMD...` → 行 id を stdout に 1 行出力
    - `seasalt exit --session SESS --last-id ID --code CODE` → 出力なし
    - `seasalt suggest --cwd DIR -- LINE...` → 候補全文 or 空
    - `seasalt search [--cwd DIR] [--all] [--limit N] PATTERN` → デフォルトは `cmd` のみ 1 行ずつ、`--tsv` で `id\tcwd\tcmd\texit_code\tstarted_at`
    - `seasalt init bash` → Task 4 で実装。このタスクでは clap に `Init` バリアントを宣言しない。

- [ ] **Step 1: 失敗するテストを書く**

```rust
// tests/cli_test.rs
use std::process::Command;

fn bin() -> Command {
    Command::new(env!("CARGO_BIN_EXE_seasalt"))
}

fn temp_data_dir() -> std::path::PathBuf {
    let dir = std::env::temp_dir().join(format!("seasalt-cli-{}", std::process::id()));
    let _ = std::fs::remove_dir_all(&dir); // テスト用の一時ディレクトリなので削除してよい
    std::fs::create_dir_all(&dir).unwrap();
    dir
}

#[test]
fn record_then_exit_then_search() {
    let dir = temp_data_dir();
    bin().env("SEASALT_DATA_DIR", &dir)
        .args(["record", "--cwd", "/tmp/x", "--session", "s1", "--", "echo hello"])
        .status().unwrap();
    bin().env("SEASALT_DATA_DIR", &dir)
        .args(["exit", "--session", "s1", "--last-id", "1", "--code", "0"])
        .status().unwrap();

    let out = bin().env("SEASALT_DATA_DIR", &dir)
        .args(["search", "--tsv", "hello"])
        .output().unwrap();
    assert!(out.status.success());
    let text = String::from_utf8(out.stdout).unwrap();
    // tsv 形式: id\tcwd\tcmd\texit_code\tstarted_at (started_at は実時刻のため後半のみ確認)
    let fields: Vec<&str> = text.trim().split('\t').collect();
    assert_eq!(fields[0..4], ["1", "/tmp/x", "echo hello", "0"]);
    assert!(!fields[4].is_empty());
}

#[test]
fn search_filters_by_cwd() {
    let dir = temp_data_dir();
    for (cwd, cmd) in [("/a", "one"), ("/b", "two")] {
        bin().env("SEASALT_DATA_DIR", &dir)
            .args(["record", "--cwd", cwd, "--session", "s1", "--", cmd])
            .status().unwrap();
    }
    let out = bin().env("SEASALT_DATA_DIR", &dir)
        .args(["search", "--cwd", "/a", "o"])
        .output().unwrap();
    let text = String::from_utf8(out.stdout).unwrap();
    assert_eq!(text.trim(), "one");
}

#[test]
fn suggest_end_to_end() {
    let dir = temp_data_dir();
    bin().env("SEASALT_DATA_DIR", &dir)
        .args(["record", "--cwd", "/proj", "--session", "s1", "--", "cargo build"])
        .status().unwrap();
    let out = bin().env("SEASALT_DATA_DIR", &dir)
        .args(["suggest", "--cwd", "/proj", "--", "cargo"])
        .output().unwrap();
    assert_eq!(String::from_utf8(out.stdout).unwrap().trim(), "cargo build");

    // 候補なし → 空出力・成功終了
    let out = bin().env("SEASALT_DATA_DIR", &dir)
        .args(["suggest", "--cwd", "/proj", "--", "nothing"])
        .output().unwrap();
    assert!(out.status.success());
    assert_eq!(out.stdout, b"");
}

#[test]
fn record_prints_row_id() {
    let dir = temp_data_dir();
    let out = bin().env("SEASALT_DATA_DIR", &dir)
        .args(["record", "--cwd", "/x", "--session", "s1", "--", "cmd"])
        .output().unwrap();
    let id: i64 = String::from_utf8(out.stdout).unwrap().trim().parse().unwrap();
    assert!(id > 0);
}
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cargo test`
Expected: FAIL(`main` が未実装のためサブコマンドが無い)

- [ ] **Step 3: 最小実装**

```rust
// src/search.rs
use anyhow::Result;
use rusqlite::Connection;

use crate::db::HistoryEntry;

pub fn search(
    conn: &Connection,
    cwd: Option<&str>,
    pattern: &str,
    limit: usize,
) -> Result<Vec<HistoryEntry>> {
    let like = format!("%{}%", pattern);
    let mut stmt = if let Some(dir) = cwd {
        conn.prepare(
            "SELECT id, cwd, cmd, exit_code, started_at FROM history
             WHERE cwd = ?1 AND cmd LIKE ?2
             ORDER BY started_at DESC, id DESC LIMIT ?3",
        )?
    } else {
        conn.prepare(
            "SELECT id, cwd, cmd, exit_code, started_at FROM history
             WHERE cmd LIKE ?1
             ORDER BY started_at DESC, id DESC LIMIT ?2",
        )?
    };
    let rows = if let Some(dir) = cwd {
        stmt.query_map(rusqlite::params![dir, like, limit as i64], map_row)?
    } else {
        stmt.query_map(rusqlite::params![like, limit as i64], map_row)?
    };
    let mut out = Vec::new();
    for row in rows {
        out.push(row?);
    }
    Ok(out)
}

fn map_row(r: &rusqlite::Row<'_>) -> rusqlite::Result<HistoryEntry> {
    Ok(HistoryEntry {
        id: r.get(0)?,
        cwd: r.get(1)?,
        cmd: r.get(2)?,
        exit_code: r.get(3)?,
        started_at: r.get(4)?,
    })
}
```

```rust
// src/main.rs
use std::process::ExitCode;

use anyhow::Result;
use clap::{Parser, Subcommand};

#[derive(Parser)]
#[command(name = "seasalt", version, about = "fish-style autosuggestion and per-directory history for bash")]
struct Cli {
    #[command(subcommand)]
    command: Command,
}

#[derive(Subcommand)]
enum Command {
    /// Record a command into history (preexec hook)
    Record {
        #[arg(long)]
        cwd: String,
        #[arg(long)]
        session: String,
        #[arg(trailing_var_arg = true, required = true, allow_hyphen_values = true)]
        cmd: Vec<String>,
    },
    /// Update the exit code of a recorded command (precmd hook)
    Exit {
        #[arg(long)]
        session: String,
        #[arg(long)]
        last_id: i64,
        #[arg(long)]
        code: i64,
    },
    /// Suggest the best history match for the current line
    Suggest {
        #[arg(long)]
        cwd: String,
        #[arg(trailing_var_arg = true, required = true, allow_hyphen_values = true)]
        line: Vec<String>,
    },
    /// Search history
    Search {
        #[arg(long, conflicts_with = "all")]
        cwd: Option<String>,
        #[arg(long)]
        all: bool,
        #[arg(long, default_value_t = 100)]
        limit: usize,
        #[arg(long)]
        tsv: bool,
        pattern: String,
    },
}

fn main() -> ExitCode {
    let cli = Cli::parse();
    match run(cli) {
        Ok(()) => ExitCode::SUCCESS,
        Err(e) => {
            // 静かに失敗: シェル統合から呼ばれるため stderr は出さない
            let _ = e;
            ExitCode::FAILURE
        }
    }
}

fn run(cli: Cli) -> Result<()> {
    let path = seasalt::db::default_db_path()?;
    let conn = seasalt::db::open(&path)?;
    match cli.command {
        Command::Record { cwd, session, cmd } => {
            let cmd = cmd.join(" ");
            let started_at = now_ms();
            let id = seasalt::db::insert_history(&conn, &cwd, &cmd, started_at, &session)?;
            println!("{id}");
        }
        Command::Exit { session, last_id, code } => {
            seasalt::db::update_exit_code(&conn, &session, last_id, code)?;
        }
        Command::Suggest { cwd, line } => {
            let line = line.join(" ");
            if let Some(cmd) = seasalt::suggest::suggest(&conn, &cwd, &line)? {
                println!("{cmd}");
            }
        }
        Command::Search { cwd, all, limit, tsv, pattern } => {
            let cwd_filter = if all { None } else { cwd.as_deref() };
            let entries = seasalt::search::search(&conn, cwd_filter, &pattern, limit)?;
            for e in entries {
                if tsv {
                    let code = e.exit_code.map(|c| c.to_string()).unwrap_or_default();
                    println!("{}\t{}\t{}\t{}\t{}", e.id, e.cwd, e.cmd, code, e.started_at);
                } else {
                    println!("{}", e.cmd);
                }
            }
        }
    }
    Ok(())
}

fn now_ms() -> i64 {
    std::time::SystemTime::now()
        .duration_since(std::time::UNIX_EPOCH)
        .map(|d| d.as_millis() as i64)
        .unwrap_or(0)
}
```

```rust
// src/lib.rs
pub mod db;
pub mod search;
pub mod suggest;
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cargo test`
Expected: PASS(4 統合テスト)

- [ ] **Step 5: コミット(ユーザー承認を得てから)**

```bash
git add src/lib.rs src/search.rs src/main.rs tests/cli_test.rs
git commit -m "feat: add record, exit, suggest, and search subcommands"
```

---

### Task 4: flake.nix

**Files:**
- Create: `flake.nix`

**Interfaces:**
- Consumes: `Cargo.toml`, `Cargo.lock`(Task 1 で生成済み)
- Produces: `nix build .#default` で `seasalt` バイナリをビルドする flake

- [ ] **Step 1: ファイルを作成**

```nix
# flake.nix
{
  description = "seasalt - fish-style autosuggestion and per-directory history for bash";

  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";
    flake-utils.url = "github:numtide/flake-utils";
  };

  outputs = { self, nixpkgs, flake-utils }:
    flake-utils.lib.eachDefaultSystem (system:
      let
        pkgs = nixpkgs.legacyPackages.${system};
      in
      {
        packages.default = pkgs.rustPlatform.buildRustPackage {
          pname = "seasalt";
          version = "0.1.0";
          src = ./.;
          cargoLock.lockFile = ./Cargo.lock;
        };
      });
}
```

- [ ] **Step 2: ビルド検証**

Run: `nix build .#default`
Expected: 成功(初回は nixpkgs の fetch と Rust 依存ビルドで数分かかる)。`result/bin/seasalt --help` が動くこと。

- [ ] **Step 3: コミット(ユーザー承認を得てから)**

```bash
git add flake.nix
git commit -m "feat: add nix flake"
```

---

### Task 5: ble.sh 統合(init bash / bash スニペット / smoke test)

**Files:**
- Create: `src/bash/seasalt.bash`(include_str! で埋め込む統合スニペット)
- Create: `src/integration.rs`
- Modify: `src/lib.rs`(`pub mod integration;`)
- Modify: `src/main.rs`(`Init { shell }` サブコマンドを追加)
- Create: `tests/bash/smoke.sh`(ble.sh なしでスニペットを検証する bash テスト)

**Interfaces:**
- Consumes: Task 1〜3 の CLI
- Produces:
  - `seasalt init bash` — bash 統合スニペットを stdout に出力(eval 可能)。`SEASALT_BIN` 環境変数があれば seasalt のパス解決に使う(テスト用)
  - `integration::bash_init_script() -> &'static str` — `include_str!("bash/seasalt.bash")`
  - スニペットが定義する関数:
    - `_seasalt_preexec` — PREEXEC フック。`$1` をコマンドとして `seasalt record` を呼び、返った id を `_seasalt_last_id` に保持
    - `_seasalt_precmd` — PRECMD フック。先頭で `$?` を捕捉し `_seasalt_last_id` があれば `seasalt exit` を呼んで解除
    - `ble/complete/auto-complete/source:seasalt` — ble.sh の auto-complete ソース。`_ble_edit_str` を `seasalt suggest` に渡し、候補があれば `ble/complete/auto-complete/enter h 0 "$suggest" '' "$cmd"` を呼ぶ。なしなら 1 を返す

**ble.sh の事実(このタスクの前提):**
- `blehook PREEXEC+=fn` — コマンド実行前に呼ばれ、`$1` にコマンド文字列が入る
- `blehook PRECMD+=fn` — コマンド実行後・プロンプト前に呼ばれるが、`$?` は保持されないため関数先頭で捕捉する
- `_ble_complete_auto_source=(history syntax)` — 配列に自作ソース名を追加すると auto-complete が呼ぶ(`ble/complete/auto-complete/source:<name>` 関数を定義する)
- `ble/complete/auto-complete/enter h 0 "$suggest" '' "$cmd"` — 灰色サジェスト描画と → キー確定(標準キーマップ)を有効にする

- [ ] **Step 1: 統合スニペットを作成**

```bash
# src/bash/seasalt.bash
# seasalt bash integration (generated by `seasalt init bash`)
if command -v "${SEASALT_BIN:-seasalt}" >/dev/null 2>&1 || [[ -n ${SEASALT_BIN:-} ]]; then
  _seasalt_bin=${SEASALT_BIN:-seasalt}

  _seasalt_session="$$-$RANDOM-$RANDOM"
  _seasalt_last_id=

  function _seasalt_preexec {
    local cmd=${1:-}
    [[ $cmd ]] || return 0
    _seasalt_last_id=$("$_seasalt_bin" record --cwd "$PWD" --session "$_seasalt_session" -- "$cmd" 2>/dev/null)
    case $_seasalt_last_id in
      (''|*[!0-9]*) _seasalt_last_id= ;;
    esac
  }

  function _seasalt_precmd {
    local code=$?
    if [[ $_seasalt_last_id ]]; then
      "$_seasalt_bin" exit --session "$_seasalt_session" --last-id "$_seasalt_last_id" --code "$code" >/dev/null 2>&1
      _seasalt_last_id=
    fi
  }

  if declare -F blehook >/dev/null 2>&1; then
    blehook PREEXEC+=_seasalt_preexec
    blehook PRECMD+=_seasalt_precmd

    if (( ${#_ble_complete_auto_source[@]} )); then
      [[ :${_ble_complete_auto_source[*]}: == *:seasalt:* ]] || _ble_complete_auto_source=(seasalt "${_ble_complete_auto_source[@]}")
    else
      _ble_complete_auto_source=(seasalt history syntax)
    fi

    function ble/complete/auto-complete/source:seasalt {
      [[ ${_ble_edit_str:-} ]] || return 1
      ((_ble_edit_ind == ${#_ble_edit_str})) || return 1
      local cmd suggest
      cmd=$(timeout 0.2 "$_seasalt_bin" suggest --cwd "$PWD" -- "$_ble_edit_str" 2>/dev/null) || return 1
      [[ $cmd ]] || return 1
      [[ $cmd == "$_ble_edit_str" ]] && return 1
      suggest=${cmd:${#_ble_edit_str}}
      ble/complete/auto-complete/enter h 0 "$suggest" '' "$cmd"
    }
  fi
fi
```

```rust
// src/integration.rs
pub fn bash_init_script() -> &'static str {
    include_str!("bash/seasalt.bash")
}
```

```rust
// src/lib.rs に追加
pub mod integration;
```

```rust
// src/main.rs の Command enum に追加
    /// Emit shell integration code (bash)
    Init {
        shell: String,
    },
```

```rust
// src/main.rs の run() の match に追加
        Command::Init { shell } => match shell.as_str() {
            "bash" => print!("{}", seasalt::integration::bash_init_script()),
            other => anyhow::bail!("unsupported shell: {other}"),
        },
```

- [ ] **Step 2: 失敗するテストを書く**

```rust
// tests/bash_test.rs
use std::io::Write;
use std::process::{Command, Stdio};

#[test]
fn init_bash_output_is_valid_bash_syntax() {
    let out = Command::new(env!("CARGO_BIN_EXE_seasalt"))
        .args(["init", "bash"])
        .output()
        .unwrap();
    assert!(out.status.success());
    assert!(!out.stdout.is_empty());

    let mut child = Command::new("bash")
        .args(["-n"])
        .stdin(Stdio::piped())
        .stdout(Stdio::null())
        .stderr(Stdio::piped())
        .spawn()
        .unwrap();
    child.stdin.as_mut().unwrap().write_all(&out.stdout).unwrap();
    let result = child.wait_with_output().unwrap();
    assert!(result.status.success(), "bash -n が失敗: {}", String::from_utf8_lossy(&result.stderr));
}

#[test]
fn init_bash_rejects_unknown_shell() {
    let out = Command::new(env!("CARGO_BIN_EXE_seasalt"))
        .args(["init", "zsh"])
        .output()
        .unwrap();
    assert!(!out.status.success());
}
```

```bash
# tests/bash/smoke.sh
#!/usr/bin/env bash
# seasalt 統合スニペットの smoke test(ble.sh なしで実行可能)
set -euo pipefail

BIN="${1:?usage: smoke.sh <path-to-seasalt-binary>}"
export SEASALT_BIN="$BIN"
export SEASALT_DATA_DIR="$(mktemp -d)"

# ble.sh のふりをする関数。登録内容を記録するだけ
BASHER_HOOKS=()
blehook() { BASHER_HOOKS+=("$*"); }

eval "$("$BIN" init bash)"

# フック登録確認
[[ ${#BASHER_HOOKS[@]} -eq 2 ]] || { echo "FAIL: hooks not registered: ${BASHER_HOOKS[*]}"; exit 1; }

# preexec → record
_seasalt_preexec "echo hello world"
[[ $_seasalt_last_id =~ ^[0-9]+$ ]] || { echo "FAIL: last id not numeric: $_seasalt_last_id"; exit 1; }

# precmd → exit code update
_seasalt_precmd

# 履歴確認
OUT=$("$BIN" search --tsv hello)
[[ $OUT == *"$PWD"*"echo hello world"*"0"* ]] || { echo "FAIL: history missing: $OUT"; exit 1; }

# suggest 確認
SUGG=$("$BIN" suggest --cwd "$PWD" -- "echo")
[[ $SUGG == "echo hello world" ]] || { echo "FAIL: suggest mismatch: $SUGG"; exit 1; }

# auto-complete ソースが定義されていること
declare -F ble/complete/auto-complete/source:seasalt >/dev/null || { echo "FAIL: source fn missing"; exit 1; }

echo "OK"
```

- [ ] **Step 3: テストが失敗することを確認**

Run: `cargo test`(bash_test は失敗はしない — init 未実装だと non-zero で終わるため `init_bash_output_is_valid_bash_syntax` が FAIL)、および:
Run: `bash tests/bash/smoke.sh target/debug/seasalt`(ビルド後)
Expected: FAIL(init サブコマンドが無い)

- [ ] **Step 4: 実装してテストを通す**

Run: `cargo test` と `bash tests/bash/smoke.sh target/debug/seasalt`
Expected: PASS(4 + 2 テスト、smoke test は `OK`)

- [ ] **Step 5: コミット(ユーザー承認を得てから)**

```bash
git add src/bash/seasalt.bash src/integration.rs src/lib.rs src/main.rs tests/bash_test.rs tests/bash/smoke.sh
git commit -m "feat: add ble.sh integration via init bash"
```

---

### Task 6: 実機導入と検証(M2 完了)

**Files:**
- Modify: `~/.bashrc`(ユーザー承認を得てから、`ble.sh` の source 行の後ろに追加)

**Interfaces:**
- Consumes: Task 5 の `seasalt init bash`
- Produces: 動作確認済みの実環境

- [ ] **Step 1: .bashrc への追加(必ずユーザーに確認してから)**

```bash
# ~/.bashrc の ble.sh source 行の後ろに追記
eval "$(command -v seasalt >/dev/null 2>&1 && seasalt init bash)"
```

※ nix 環境の場合は flake 由来のパスをそのまま使うか、nix profile に追加する。

- [ ] **Step 2: 実機確認チェックリスト**

新規シェルを開いて以下を確認する:
- [ ] タイピング中に履歴由来の灰色サジェストが表示される
- [ ] → キーでサジェストが確定される
- [ ] フォルダを跨いだ場合、現在のフォルダ(とその親)の履歴が優先される
- [ ] `seasalt search git` でフォルダ別・全体の検索ができる
- [ ] atuin の Ctrl-R と共存して干渉しない(atuin の履歴検索が従来通り動く)
- [ ] 未補完時に Tab 補完(ble.sh 標準)が従来通り動く

- [ ] **Step 3: 性能計測**

```bash
# suggest の応答時間を計測(1ms 前後が理想、10ms を超えたら非同期化を検討)
time for i in $(seq 1 20); do
  seasalt suggest --cwd "$PWD" -- "git" >/dev/null
done
```

計測結果をユーザーに報告する。問題がなければ M2 完了とする。

---

### Task 7: 仕上げ

**Files:**
- Modify: `README.md`(作成)

- [ ] **Step 1: README を作成**

インストール方法(`cargo install --path .` / `nix build .#default`)、`.bashrc` への追記方法、サブコマンドの説明、atuin との共存方針、既知の制限(ble.sh 必須、M3 の Ctrl-R 検索 UI は未実装)を書く。

- [ ] **Step 2: 最終検証**

Run: `cargo fmt && cargo check && cargo clippy && cargo test`
Expected: すべて成功、clippy の警告なし

- [ ] **Step 3: コミット(ユーザー承認を得てから)**

```bash
git add README.md
git commit -m "docs: add README"
```

---

## Scope Note (今回の対象外)

- M3(Ctrl-R 検索 UI、fzf 連携 or ratatui)と M4(Tab 補完の fish 風化)は spec 通り今回の実装対象外。Task 6 の性能計測で非同期化が必要と判明した場合は、M3 着手前に個別プランを作る。
